age = int(input("Enter age:"))

if age > 28:
    print(" can't appear for exam")
    print(" use backend to appear for exam")
    # this block will carry out even though process should be stopped

else:
    print(" appear for exam")